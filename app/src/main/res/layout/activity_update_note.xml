<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    tools:context=".UpdateNoteActivity">

    <TextView
        android:id="@+id/labelEditNote_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginTop="16dp"
        android:fontFamily="@font/poppins"
        android:text="@string/update_note"
        android:textColor="@color/main"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/saveEdit_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="8dp"
        android:src="@drawable/ic_done"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <EditText
        android:id="@+id/labelEditTitle_edt"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:hint="@string/labelTitleNote"
        android:padding="12dp"
        android:textSize="20sp"
        android:layout_marginTop="16dp"
        android:background="@drawable/main_border"
        android:maxLines="1"
        android:fontFamily="@font/poppins"
        app:layout_constraintTop_toBottomOf="@+id/labelEditNote_tv" />

    <EditText
        android:id="@+id/contentEditNote_edt"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/main_border"
        android:fontFamily="@font/poppins"
        android:padding="12dp"
        android:textSize="18sp"
        android:hint="@string/contentNote"
        android:gravity="top"
        android:layout_marginTop="150dp"
        app:layout_constraintTop_toBottomOf="@+id/labelEditTitle_edt" />

</androidx.constraintlayout.widget.ConstraintLayout>